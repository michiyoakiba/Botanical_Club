<div class="container">
  <div class="row justify-center">
    <div class="col-12">  
      <table class="table table-borderless">
          
        <thead>
          <tr>
            <th>image</th>
            <th>name</th>
            <th colspan="3"></th>
          </tr>
        </thead> 
        
        <tbody>    
          <tr>
          <% users.each do |user| %> 
           <th><%= image_tag user.get_profile_image, size: '100x100', class:"rounded-circle"; %></th>
           <th><%= user.name %></th>
           
           <th>フォロー数: <%= user.followings.count %></th>
           <th>フォロワー数: <%= user.followers.count %></t>
           <th><%= render "relationships/btn", user: user %></th>
          </tr>
          <% end %>
        </tbody>
        
      </table>
    </div>
  </div>
</div>

